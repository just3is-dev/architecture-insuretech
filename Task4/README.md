# Задание 4 — примечание

Ссылка на библиотеку обозначений паттернов отказоустойчивости из TASKS.md (строка 161,
`https://code.s3.yandex.net/software-architect/Обозначения%20паттерном%20отказоустойчивости.drawio.xml`)
не работает — сервер возвращает `NoSuchKey` (404). Проверено несколько вариантов написания
имени файла, все недоступны; другие ссылки в том же бакете (`code.s3.yandex.net/software-architect/...`)
при этом открываются нормально, то есть это битая ссылка в самом источнике задания, а не сетевая проблема.

Поскольку официальная библиотека иконок недоступна, применение паттернов (Rate Limiting,
Circuit Breaker, Retry, Timeout) на диаграмме `InsureTech_диаграмма_контейнеров_to-be.drawio.xml`
отмечено простыми текстовыми бейджами рядом с соответствующими связями, а не иконками из
библиотеки.
